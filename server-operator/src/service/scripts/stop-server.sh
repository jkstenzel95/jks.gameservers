# Get PID for process from ephemeral root disk file (Should exist!)
# If doesn't exist, log an error, idk if to exit error, probably not
# Server specific stop code with PID as input
# Remove PID file